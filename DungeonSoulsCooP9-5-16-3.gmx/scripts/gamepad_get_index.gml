/**
    Obtains the index of the gamepad in the specified slot.
    For example, if one where to obtain the index of the first connected gamepad,
    one would do:
    
    gamepad_get_index(0);
    
    For the second one:
    
    gamepad_get_index(1);
    
    And so on.
    
    Returns -1 if there isn't any gamepad connection on the specified index.
*/
return gamepad_index[argument0];
