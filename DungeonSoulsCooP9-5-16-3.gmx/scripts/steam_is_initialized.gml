///steam_is_initialized()
/*
    Checks whether the steam was properly initialized by looking up a variable's value
    from a past search. This way, no inspections are performed when checking for the
    value. Works as a "get"
*/
return steam_enabled;
